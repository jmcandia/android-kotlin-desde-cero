# CONTEXT.md — Reestructuración del curso

Registro de trabajo de la reestructuración del curso «Android con Kotlin desde cero». Se actualiza al terminar cada tarea, para poder retomar el trabajo si una sesión se interrumpe.

## Decisiones acordadas

| Tema | Decisión |
|---|---|
| Proyecto final | App de **contactos** contra `code/contact-list-api` (reemplaza a la Pokédex) |
| Room | Primero en la app de tareas (cap. 47 + tutorial 5); luego como segunda fuente en el proyecto final |
| Hilt | Obligatorio (después del contenedor manual) |
| Archivos | Renombrados con la nueva numeración (`partNN/chapterNN.md` = número global) |
| Anexo B | Se amplía a «De Java a Kotlin: equivalencias y `Scanner`» |
| Fases | Se ejecutan todas |

## Nueva estructura

| Parte | Capítulos | Contenido |
|---|---|---|
| I · Fundamentos de Kotlin | 1–7 | Sin cambios de estructura |
| II · Control de flujo y funciones | 8–10 | Sin cambios de estructura |
| III · Colecciones y código robusto | 11–14 | `String.format` pasa al anexo D |
| IV · Programación orientada a objetos | 15–21 | 20 = genéricos y extensiones; **21 = lambdas a fondo (nuevo)** |
| V · Coroutines y Flow | 22–24 | — |
| VI · Android y arquitectura | 25–27 | 26 = ciclo de vida; **27 = arquitectura conceptual (nuevo)** |
| VII · Jetpack Compose | 28–37 | 28 primer composable · 29 `Modifier` y layouts · 30 recursos · 31 Material 3 · 32 estado · 33 `Scaffold`/`LazyColumn` · tutorial 1 · 34 formularios · 35 navegación · 36 tema · tutorial 2 · 37 diseño adaptable (opcional) |
| VIII · Estado y arquitectura con MVVM | 38–42 | 38 MVVM · 39 ViewModel/UiState · **40 efectos y eventos (nuevo)** · tutorial 3 · 41 repositorio · 42 inyección (Hilt) · tutorial 4 |
| IX · Datos: REST y Room | 43–47 | 43 HTTP/REST · 44 JSON/DTO · 45 Retrofit · 46 estados de red · **47 Room (nuevo)** · tutorial 5 |
| X · Proyecto final: contactos | 48–58 | Todo nuevo |
| Anexos | A–D | A principios de diseño · B Java→Kotlin y `Scanner` · C SDK · D `String.format` |

## Tareas realizadas

### 2026-09-22 — Sesión 1 (interrumpida)
- Análisis pedagógico del curso y plan de rediseño en 10 partes (el plan completo quedó en el transcript de la sesión).
- Renombrado de archivos con `git mv` a la nueva numeración; se eliminaron los esbozos de la Pokédex (antiguos 42–51).
- Imágenes movidas: `chapter30/layout-column-row-box.svg` → `chapter29/`, `chapter30/scaffold.svg` → `chapter33/`.
- Captura del JSON real de la API (lista paginada HAL, detalle, errores).
- App de tareas validada por etapas (en un directorio temporal, no en el repo):
  - etapa 3: `TareasViewModel`;
  - etapa 4: repositorio en memoria + Hilt 2.60.1 / KSP 2.3.12 / AGP 9.4.1, con 3 pruebas unitarias;
  - etapa 5: Room, probada en el emulador (API 37).
- App «Mis Contactos» escrita (capa de datos, repositorio Retrofit + Room, módulos Hilt, interfaz). La compilación se cortó por falta de memoria.

### 2026-09-22 — Sesión 2
- **App de contactos** copiada a `code/contact-list-app/`:
  - compila (`assembleDebug`) sin advertencias; lint: 0 errores;
  - `hiltViewModel` migrado a `androidx.hilt:hilt-lifecycle-viewmodel-compose` (el de `hilt-navigation-compose` está obsoleto);
  - Kotlin 2.4.20 (2.2.10 fallaba con las dependencias);
  - permiso `ACCESS_LOCAL_NETWORK` en el manifiesto + `ui/componentes/PermisoRedLocal.kt`, que lo pide en tiempo de ejecución;
  - probada en el emulador: permiso → lista desde la API → detalle → favorito.
- **Redistribución de la Parte VII**, solo estructura, con el contenido existente:
  - 26 queda solo con `Activity` y ciclo de vida;
  - 28 (nuevo): segunda mitad del 26 + composables con parámetros del antiguo 27;
  - 29 (nuevo): `Modifier` del antiguo 27 + layouts básicos del 33;
  - 31 sin formularios; 32 + recomposición + `TextField`/`Checkbox`/`Switch`;
  - 33 solo `Scaffold`, `LazyColumn` y jerarquías;
  - 34 (nuevo): formularios; 35: barras de navegación al final;
  - 37 (nuevo, opcional): *Window Size Classes*.
- **Títulos** `# Capítulo N` renumerados; anexos con letra (A–D); referencias cruzadas corregidas en los tutoriales 1 y 2.
- **`mkdocs.yml`**: `nav` reescrito con la nueva estructura; `mkdocs build --strict` pasa.
- Creado este `CONTEXT.md`.
- **Cap. 20** reducido a genéricos y funciones de extensión (la sección de lambdas pasa al 21).
- **Cap. 21 (nuevo)**: lambdas a fondo. Sin repetir lo del cap. 10 (tipos de función, orden superior, *trailing lambda*), cubre referencias con `::`, captura de variables, lambdas como parámetros opcionales, lambdas con receptor (con el vínculo a `RowScope` en Compose), funciones de alcance y delegación con `by` (`lazy`, `remember`, interfaces). Todos los ejemplos se compilaron y ejecutaron con `kotlinc`; agregado al `nav`.
- **Cap. 27 (nuevo)**: cómo se organiza una app (91 líneas, sin código). Cinco trabajos (mostrar, reaccionar, decidir, obtener, guardar) → tres zonas (interfaz, estado y lógica, datos), flujo unidireccional con el ejemplo de la lista de tareas, dependencias hacia los datos, mapa Compose/ViewModel/repositorio = MVVM y aviso «responsabilidades, no carpetas». Cierre del 26 e introducción del 28 enlazados; agregado al `nav`.
- **Cap. 40 (nuevo)**: efectos y eventos puntuales. Por qué no hay efectos en el cuerpo de un composable; `LaunchedEffect` (lanzar, cancelar, reiniciar por clave); Snackbar (`SnackbarHostState`, `SnackbarHost`, `showSnackbar`, `rememberCoroutineScope` para toques); eventos del `ViewModel` **modelados como estado** (`mensaje: Mensaje?` + `mensajeMostrado()`, igual que la etapa 3 validada y la app de contactos); `SharedFlow` como alternativa con el riesgo de perder eventos; navegar después de guardar con `guardado = true`. Fragmentos compilados en una copia temporal de la app. Cierre del 39 e introducción del 41 ajustados; agregado al `nav`.
- **Cap. 47 (nuevo)**: persistencia local con Room, con un dominio neutro (**notas**) para no adelantar el tutorial 5. Configuración con KSP y el plugin de Room (`schemaDirectory`); entidad y mapeo `aDominio()`/`aEntity()`; DAO (`Flow` que se vuelve a emitir, operaciones `suspend`, `@Query` comprobada al compilar); `@Database`; creación con Hilt usando **`@Provides`** + `@Singleton` + `@ApplicationContext` (el cap. 42 todavía no enseña `@Provides`); Room como otra implementación del repositorio (`@Binds`); `stateIn` con `WhileSubscribed(5_000)`; migraciones y aviso sobre `fallbackToDestructiveMigration`. Código compilado con KSP en una copia temporal. Cierre del 46 ajustado; agregado al `nav`.
- **App de tareas copiada a `code/todo-list-app/`** desde el directorio temporal de la sesión 1:
  - el proyecto raíz es la etapa 5 (Room); las etapas 3 (ViewModel, sin Hilt) y 4 (repositorio en memoria + Hilt) quedan en `etapas/`, con un `README.md` que explica cómo compilarlas;
  - `hiltViewModel` migrado a `hilt-lifecycle-viewmodel-compose`, igual que en la app de contactos (también en la etapa 4);
  - `.kotlin` agregado al `.gitignore` de las dos apps;
  - verificado: la etapa 5 compila sin advertencias y pasa sus 3 pruebas unitarias; la etapa 4 compila y pasa sus 3 pruebas; la etapa 3 compila.

### 2026-09-23 — Sesión 3
- **Tutorial 3** (`part08/tutorial_my_todo_list_part_3.md`, después del cap. 40): la app de tareas pasa a MVVM sin Hilt, con el código de `etapas/etapa3/`. Pasos: comprobar que las tareas se pierden al girar (cubre lo que el cap. 27 anuncia) → dependencias `lifecycle-viewmodel-compose` y `lifecycle-runtime-compose` (reutilizando la versión `lifecycleRuntimeKtx` de la plantilla) → textos → `TareasUiState` como `data class` (explica por qué no `sealed`) → `TareasViewModel` con `update {}` y deshacer → `FormularioNuevaTarea` con `enabled` → `ListaTareasScreen` con `uiState`, Snackbar y vista previa → `App` con `viewModel()` y referencias ligadas (`viewModel::agregarTarea`, explicadas porque el cap. 21 solo muestra `Tipo::función`) → prueba, incluido el giro con el Snackbar visible. Agregado al `nav`; `mkdocs build --strict` pasa.
- **Tutorial 4** (`part08/tutorial_my_todo_list_part_4.md`, después del cap. 42), con el código de `etapas/etapa4/`: configuración de Hilt + KSP en el catálogo y los dos `build.gradle.kts` (más `kotlinx-coroutines-test`) → `MiListaDeTareasApp` con `@HiltAndroidApp` + `android:name` en el manifiesto + `@AndroidEntryPoint` → interfaz `TareasRepository` (por qué `Flow`, `suspend` y sin valor de retorno) → `TareasRepositoryEnMemoria` `@Singleton` → `DataModule` con `@Binds` → `ViewModel` `@HiltViewModel` que recolecta el repositorio en `init` (con diagrama de secuencia) → `hiltViewModel()` → 3 pruebas unitarias explicadas (`@Test`, `runTest`, `setMain`/`UnconfinedTestDispatcher`, ejercicio de romper una prueba) → prueba final que muestra que las tareas se pierden al cerrar la app. Cierre del cap. 42 ajustado para anunciar el tutorial; agregado al `nav`; `mkdocs build --strict` pasa.
- **Tutorial 5** (`part09/tutorial_my_todo_list_part_5.md`, después del cap. 47), con el código final de `code/todo-list-app/`: diagrama «antes/ahora» del repositorio → Room sobre el KSP que ya estaba → `TareaEntity` con `creadaEn` (por qué el dominio no la lleva) y mapeos → `TareaDao` (por qué `actualizar` es una `@Query` y no `@Update`: conservar `creadaEn` y el orden) → `TareasDatabase` → `DatabaseModule` con `@Provides` → `TareasRepositoryRoom` (los dos `map`) → cambio de una línea en `DataModule` → «lo que no cambió» (ViewModel, pantallas, pruebas con el repositorio en memoria; nota sobre `stateIn` del cap. 47 frente a `collect` en `init`) → prueba de cierre/reinicio, Database Inspector y el esquema `1.json` en el control de versiones. Cierre que enlaza con la Parte X. Agregado al `nav`; `mkdocs build --strict` pasa.
- **Orden de trabajo:** la Parte X depende de contenido que el plan asigna a los caps. 43–46 y que todavía no estaba (CRUD con Retrofit, *cleartext*, `10.0.2.2`, tipos de error). Por eso se reescriben primero los caps. 43–46 (parte de la tarea de reescrituras) y después se escribe la Parte X.
- **Cap. 43 ampliado**: cuerpo en `POST`/`PUT`, tabla de códigos (200, 201, 204, 400, 404, 409, 500) con qué hace la app en cada caso, `PUT` y parámetros de consulta en REST, y una práctica de 8 pasos con la API del curso en Swagger UI (arranque con `./mvnw`/`mvnw.cmd`, JSON HAL real del detalle, 404, lista paginada con `_embedded`/`page`, búsqueda sin resultados sin `_embedded`, 201 sin claves nulas, 409, 400 con `errors[]`, 204). Todas las respuestas se capturaron de la API en ejecución. Cierre apunta al cap. 44 (serialización), ya no a Retrofit.
- **Cap. 44 reescrito** (serialización, ahora antes de Retrofit): configuración del plugin y la biblioteca en el catálogo; `ContactDto` real; `ignoreUnknownKeys` mostrado con el error real por `_links`; claves que faltan → `String? = null` (con el `MissingFieldException` real); `@SerialName` para `_embedded`/`contactResponseList` y clases anidadas de la página; búsqueda vacía sin `_embedded`; `ApiErrorDto`; `ContactRequestDto` y `encodeToString` (las propiedades con valor por defecto no se escriben); práctica sin red con una prueba unitaria y *raw strings*; DTO frente a `Contacto` con `aDominio()`. Todo compilado y ejecutado con `kotlinc` + plugin de serialización.
- **Cap. 45 y 46 reescritos** (Retrofit y estados de red), ya con el código real de `code/contact-list-app/`: cap. 45 cubre la interfaz `ContactApi` (`@GET`/`@POST`/`@PUT`/`@DELETE`, `@Path`, `@Query` con valores por defecto y omisión de `null`, `@Body`), la instancia con `Retrofit.Builder`, el módulo `NetworkModule` con `@Provides`/`@Singleton` (contrastado con `@Binds` del cap. 42) y los tres obstáculos de conectarse a la API local (`10.0.2.2`, permiso `INTERNET`, *cleartext* con `network_security_config.xml`, y el permiso de ejecución `ACCESS_LOCAL_NETWORK` de Android 17); cap. 46 cubre la `sealed interface ContactosUiState` (cargando/éxito/error), la carga en `init`, por qué el repositorio devuelve `Result` (`onSuccess`/`onFailure`/`fold`), la clase sellada `ErrorDatos` y su traducción desde `IOException`/`HttpException` (`aErrorDatos`), el porqué de no atrapar `CancellationException`, y la interfaz reaccionando con un `when` exhaustivo y mensajes en `strings.xml`. (Este trabajo ya estaba en el árbol de trabajo sin registrar; se verificó contra el código de `code/contact-list-app/` y `mkdocs build --strict` pasa.)

### 2026-09-23 — Sesión 4
- **Cap. 39 ampliado**: agregada una nota sobre cuándo usar `data class` en vez de `sealed class` para `UiState` (estados no excluyentes), una subsección sobre cargar datos en el bloque `init` (se ejecuta una sola vez, sobrevive al giro) y una subsección sobre `update { }` como alternativa segura a `_uiState.value = ...` cuando el nuevo estado depende del anterior (con un ejemplo de `ContadorViewModel`). Resumen ajustado. Cierra la brecha que el cap. 40 y el tutorial 3 ya daban por conocida.
- **Cap. 24 ajustado**: la sección de `SharedFlow` ya no lo recomienda para eventos de interfaz (Snackbar, navegación); explica el riesgo de perder el evento durante la recreación de la pantalla y remite al cap. 40, que los modela como estado. `SharedFlow` queda descrito para eventos entre componentes con múltiples suscriptores. Resumen ajustado.
- `mkdocs build --strict` pasa después de cada cambio.
- **Investigación completa del código de `code/contact-list-app/`** para planificar la Parte X: leídos todos los archivos fuente (ViewModels, repositorio offline-first, Room con caché + favoritos locales, Retrofit, DTOs y mapeos, todas las pantallas —lista con búsqueda debounced/paginación/pull-to-refresh/favoritos, detalle con eliminación, formulario con validación local y del servidor—, navegación type-safe, componentes compartidos, módulos Hilt, manifiesto, dependencias del catálogo, strings). La app está completa y funcional; todas las funcionalidades documentadas.
- **Parte X iniciada: capítulos 48–51 escritos**:
  - Cap. 48: presentación de «Mis Contactos», arquitectura MVVM por capas, modelo de dominio (`Contacto`, `DatosContacto`, `PaginaContactos`), estructura de paquetes del proyecto, diagramas de navegación y flujo de datos.
  - Cap. 49: configuración Gradle (catálogo `libs.versions.toml`, plugins, dependencias), permisos (`INTERNET`, `ACCESS_LOCAL_NETWORK`, `network_security_config.xml`), `MisContactosApp` con `@HiltAndroidApp`, composable `PermisoRedLocal` para pedir permiso en tiempo de ejecución en Android 17+, `MainActivity` con edge-to-edge.
  - Cap. 50: DTOs (`ContactDto`, `ContactPageDto`, `ContactRequestDto`, `ApiErrorDto`), interfaz `ContactApi` con los 5 endpoints REST, mapeos DTO↔dominio (`aDominio`, `aRequest`), traductor `aErrorDatos` que convierte `IOException`/`HttpException` en `ErrorDatos`, `NetworkModule` con `@Provides` para `Json`/`OkHttpClient`/`Retrofit`/`ContactApi`.
  - Cap. 51: entidades Room (`ContactoEntity` con `orden`, `FavoritoEntity`), DAOs (`ContactoDao` con `reemplazarTodos` transaccional, `FavoritoDao` con `Flow<List<Int>>`), `ContactosDatabase`, `DatabaseModule` con `@ApplicationContext` y `@Singleton`.
  - Agregados al `nav` de `mkdocs.yml`; `mkdocs build --strict` pasa. Pendientes: caps. 52–58 (repositorio offline-first, ViewModels, pantallas, navegación, composables compartidos, cierre).

### 2026-09-23 — Sesión 5
- **Corrección al cap. 48**: al leer el código real de `model/Contacto.kt` y `data/ContactosRepositoryImpl.kt`, se detectó que el cap. 48 (escrito antes de esa lectura) inventó una propiedad `esFavorito` en `Contacto` y campos distintos en `PaginaContactos`. Corregido para reflejar el modelo real (`Contacto` sin `esFavorito`; favoritos como `Flow<Set<Int>>` aparte en el repositorio; `PaginaContactos` con `pagina`/`totalPaginas`/`desdeCache`/`hayMas` calculado). `mkdocs build --strict` pasa.
- **Cap. 52 (nuevo)**: `ContactosRepository` (interfaz con `cambios: Flow<Unit>` y `favoritos: Flow<Set<Int>>`) y `ContactosRepositoryImpl`: estrategia *offline-first* en `obtenerPagina` (caché solo en la primera página sin filtro, marcada con `desdeCache`), respaldo en `obtenerContacto`, `crear`/`actualizar`/`eliminar` siempre contra la red con aviso por `_cambios.tryEmit(Unit)`, `alternarFavorito` puramente local, y el ayudante genérico `llamarApi` (igual patrón que el cap. 46, sin atrapar `CancellationException`). Diagrama de secuencia con y sin conexión. Agregado al `nav`; `mkdocs build --strict` pasa.
- **Cap. 53 (nuevo)**: pantalla de lista. `ListaContactosUiState` como `data class` con propiedad calculada `contactosVisibles` (filtra por favoritos sin que la interfaz repita la lógica). `ListaContactosViewModel`: en `init` se suscribe a `repository.favoritos` y `repository.cambios` (se recarga sola tras crear/editar/eliminar en otra pantalla); `recargar`/`cargarMas` con `Job` cancelable y guarda contra cargas duplicadas; `cambiarBusqueda` con *debounce* de 400 ms; `alternarSoloFavoritos` puramente local; `cargarPagina` privado que reemplaza o concatena según la página. `ListaContactosScreen`/`Content`: `Scaffold` con FAB, aviso de `desdeCache`, `when` para cargando/error/lista, `PullToRefreshBox`, scroll infinito vía `LaunchedEffect(contactos.size)` en el último ítem de la `LazyColumn`, `ContactoItem` con `IconToggleButton` de favorito. Agregado al `nav`; `mkdocs build --strict` pasa.
- **Cap. 54 (nuevo)**: pantalla de detalle. `DetalleContactoUiState` como `sealed interface` (`Cargando`/`Error`/`Contenido`), con `Contenido` como `data class` que incluye el evento `eliminado: Boolean`. `DetalleContactoViewModel`: lee el `id` con `savedStateHandle.toRoute<DetalleContactoRuta>().id` (adelanto del cap. 56); `init` suscrito a `favoritos`/`cambios`; `eliminar()` marca `eliminando`→`eliminado` o `errorAccion`; ayudante privado `actualizarContenido` que solo transforma cuando el estado es `Contenido`. `DetalleContactoScreen`: `LaunchedEffect(eliminado)` navega atrás (patrón evento-como-estado del cap. 40); `AlertDialog` de confirmación antes de eliminar, con `confirmarEliminar` en `rememberSaveable`; snackbar para `errorAccion`; acciones de la `TopAppBar` (favorito/editar/eliminar) solo visibles con `contenido != null`; `FichaContacto`/`DatoContacto` muestran "sin información" para campos opcionales nulos. Agregado al `nav`; `mkdocs build --strict` pasa.
- **Cap. 55 (nuevo)**: pantalla de formulario (crear y editar). `ContactoForm` (todo `String`), `Campo` enum con `nombreApi`, `ErrorCampo` sealed interface (`Obligatorio`/`Longitud`/`LargoMaximo`/`EmailInvalido`/`Servidor`). Función pura `validarContacto` (reglas locales) y `erroresDelServidor` (traduce `"campo: mensaje"` de `ErrorDatos.Validacion` al mismo `Map<Campo, ErrorCampo>`). Mapeos `aDatos()`/`aFormulario()` entre `ContactoForm` y el dominio. `FormularioContactoViewModel`: `id: Int?` desde la ruta decide crear vs. editar; `cambiarCampo` limpia el error de ese campo; `guardar()` valida local primero (sin red si falla) y luego distingue `Validacion`/`Conflicto` (email duplicado→campo EMAIL)/otros (→`errorGeneral`). `FormularioContactoScreen`: tercer uso del patrón evento-como-estado (`guardado`+`LaunchedEffect`→volver); `CampoTexto` con `KeyboardType`/`KeyboardCapitalization` por campo y mensaje de error por campo; botón guardar deshabilitado mientras `guardando`. Agregado al `nav`; `mkdocs build --strict` pasa.
- **Mejoras pedagógicas (caps. 14 y 35) — primera iteración**:
  - **Cap. 14 reescrito**: sección sobre `throw` ampliada (excepciones estándar vs. personalizadas, `SaldoInsuficienteException`, cómo `throw` interrumpe la ejecución). Sección sobre `Result<T>` reorganizada y ampliada: ahora se presenta **primero** como tipo de retorno explícito (`Result.success`/`Result.failure`), con un ejemplo completo de `dividir(): Result<Int>`; explica cómo consumir `Result` (`isSuccess`/`isFailure`, `getOrNull()`/`exceptionOrNull()`, `getOrDefault()`, `onSuccess`/`onFailure`); enlaza con el patrón `Repository → Result<T> → ViewModel → UiState` que verá en las partes VIII/IX; y **después** presenta `runCatching` como atajo para convertir código que lanza excepciones. Nueva sección comparativa «`throw` vs. `Result`» (cuándo usar cada uno). Reorganización final: excepciones → `throw` → `try`/`catch`/`finally` → `Result<T>` → consumir `Result` → `runCatching` → comparación → resumen.
  - **Cap. 35 ampliado**: sección «Barras de navegación y destinos principales» reescrita (de 42 líneas a ~150). Nueva estructura: qué problema resuelven las barras, tabla comparativa de los tres componentes M3 (`NavigationBar`/`NavigationRail`/`ModalNavigationDrawer`), destinos principales vs. secundarios (jerarquía con ejemplos), modelar destinos con `destinosPrincipales`, sincronizar selección con `currentBackStackEntryAsState()`, evitar duplicados en la pila (explicación detallada de `popUpTo`/`launchSingleTop`/`restoreState` y por qué sin ellos el usuario retrocede 4 veces), ejemplo completo de `AppNavigationBar` + arquitectura con `Scaffold`/`NavHost`/3 destinos, adaptar al tamaño de ventana (mención de `NavigationRail` y enlace al cap. 37 opcional).
  - `mkdocs build --strict` pasa después de ambos cambios.
- **Reestructuración granular profunda (caps. 14 y 35) — segunda iteración**:
  - **Cap. 35 completamente reestructurado** siguiendo el modelo mental pedagógico detallado. Nueva progresión secuencial: (1) Modelo mental de Navigation Compose (qué es una ruta, qué es un destino, relación entre ambos); (2) ¿Qué es `NavController`? (responsabilidades, flujo de navegación Usuario→`navigate()`→NavController→NavHost→composable); (3) ¿Qué es `NavHost`? (parámetros, propósito, diagrama arquitectónico); (4) Todos los elementos juntos (ejemplo completo con diagrama); (5) Diferencia entre `composable(...)` y `navigate(...)` (declarar vs. navegar); (6) El *back stack* (cómo se apilan los destinos, `popBackStack()`); (7) Modelar rutas y destinos (sealed class); (8) Pasar datos entre pantallas (argumentos en rutas); (9) Buenas prácticas (funciones de navegación vs. pasar `NavController`); (10) Barras de navegación y destinos principales (qué problema resuelven, `NavigationBar`, `NavigationBarItem`, determinar el ítem seleccionado con `currentBackStackEntryAsState()`, evitar duplicados con `popUpTo`/`launchSingleTop`/`restoreState`, otros componentes M3 con tabla comparativa, destinos principales vs. secundarios, ejemplo completo con modelo de arquitectura, diseño adaptable). El capítulo pasó de ~334 líneas a ~550 líneas con progresión lógica estricta: conceptos fundamentales primero, APIs después, integración al final.
  - **Cap. 14 completamente reestructurado** siguiendo la progresión pedagógica detallada. Nueva secuencia: (1) ¿Qué es una excepción?; (2) `try` y `catch`; (3) `finally`; (4) Capturar excepciones específicas; (5) Lanzar tus propias excepciones con `throw` (excepciones estándar, excepciones personalizadas, `throw` interrumpe la ejecución); (6) `Result<T>`: modelar éxito o fallo como un tipo de retorno (qué es `Result<T>`, crear un `Result`, comparación `throw` vs. `Result<T>`); (7) Consumir un `Result` (propiedades de estado, extraer el valor o la excepción, reaccionar con callbacks funcionales); (8) `Result` como tipo de retorno explícito (conexión con el flujo Repository→ViewModel→UiState); (9) `runCatching`: convertir excepciones en `Result` (`runCatching` como herramienta); (10) `throw` vs. `Result`: ¿cuándo usar cada uno? (tabla comparativa con situaciones específicas); (11) ¿Cuándo usar cada herramienta? (resumen integrador); (12) Resumen. El capítulo pasó de ~152 líneas a ~380 líneas, con cada concepto explicado desde su propósito antes de mostrar su API, y con la relación entre `throw`, `Result` y `runCatching` completamente clarificada.
  - `mkdocs build --strict` pasa después de la reestructuración completa de ambos capítulos.
- **Cap. 56 (nuevo)**: navegación con rutas tipadas, contrastada explícitamente con el patrón de cadenas del cap. 35 (tabla comparativa). `Rutas.kt`: `ListaContactosRuta` (`data object`), `DetalleContactoRuta`/`FormularioContactoRuta` (`data class` con `@Serializable`, `id` obligatorio u opcional). `ContactosNavHost`: `composable<Ruta>` en vez de `composable("cadena/{id}")`, `navController.navigate(Ruta(id))` en vez de interpolar strings. `savedStateHandle.toRoute<Ruta>()` explicado a fondo (ya usado en caps. 54–55). Diagrama de flujo de navegación; sección final "¿cuándo usar cada enfoque?". Agregado al `nav`; `mkdocs build --strict` pasa.
- **Cap. 57 (nuevo)**: componentes compartidos y tema. `AvatarContacto` (Coil 3 `AsyncImage` superpuesta sobre iniciales de respaldo). `Estados.kt`: `EstadoCargando`, `EstadoError` (con `onReintentar` opcional, `null` en el detalle), `EstadoVacio` (mensaje ya armado por la pantalla), `mensajeDe(error: ErrorDatos)` con `when` exhaustivo. Mención breve de `PermisoRedLocal` (ya cubierto en el cap. 49) y por qué vive en `ui/componentes/`. `Theme.kt`/`Color.kt`: paleta propia (Azul/Turquesa) + color dinámico opcional desde Android 12, mismo patrón del cap. 36. Agregado al `nav`; `mkdocs build --strict` pasa.
- **Cap. 58 (nuevo, cierre de la Parte X)**: capítulo de síntesis, sin código nuevo. *Checklist* de pruebas manuales por pantalla (lista/detalle/formulario/generales) ligada explícitamente a las decisiones de diseño de los capítulos 52–55. Diagrama y tabla de correspondencia "Parte X ← qué parte del curso la sostiene" (I–IV, V, VI, VII, VIII, IX). Sección "qué no cubrimos a propósito" (tests automatizados, `WorkManager`, módulos Gradle múltiples, DI sin Hilt) y "próximos pasos sugeridos". Agregado al `nav`; `mkdocs build --strict` pasa.
- **Verificación completa de capítulos 16, 17, 19, 23, 25, 26, 38, 41, 42**: revisados con agente explorador. Todos están limpios: cero menciones de Pokémon/Pokédex, cero TODOs o contenido trunco, referencias cruzadas ("capítulo N", "próximo capítulo", "capítulo anterior") coherentes con la nueva numeración, y contenido alineado con la tabla de la nueva estructura (incluido el cap. 26, que quedó solo con `Activity` y ciclo de vida; el cap. 38, conceptual de MVVM; y los caps. 41 y 42 con la separación de capas y Hilt). Con esto, la tarea de reescrituras y ampliaciones de capítulos queda 100% cerrada.
- **✅ Parte X completa (capítulos 48–58, 11 capítulos)**: toda la parte fue escrita y verificada en las Sesiones 4–5, cada capítulo grounded en el código real de `code/contact-list-app/` (leído en su totalidad antes de escribir, con una corrección retroactiva al cap. 48 documentada arriba). `mkdocs build --strict` pasa tras cada capítulo.
- **`index.md`/`README.md`/`CLAUDE.md` actualizados con la nueva estructura**: `docs/index.md` reescribe la lista de partes (ahora 10, con los nombres reales del `nav` de `mkdocs.yml`) y menciona Room/favoritos/offline-first en "qué construiremos". `README.md` corrige el nombre de la app (era "MyContactListApp"/"NyContactListApp", ahora «Mis Contactos», Parte X en vez de IX) y el árbol de estructura (agrega `docs/content/`, que ya existía en el repo pero no estaba reflejado). `CLAUDE.md` corrige la nota de que `code/contact-list-app/` "aún no existe" (ya existe y está documentada en Parte X) y la referencia "Part IX" en la sección de la API. `mkdocs build --strict` pasa.
- **Tutorial 2 corregido** (Paso 11, docs/content/part07/tutorial_my_todo_list_part_2.md): agregado el paso de comprobación de rotación que el cap. 27 anticipaba y que el tutorial 3 asume. El paso 11 ahora instruye: "agrega tareas, escribe texto en el campo, gira: el texto del campo sobrevive (`rememberSaveable`), pero la lista de tareas desaparece (`remember` en `App`)". Nota explicativa al final del paso dice que no es un error, que es lo esperado al guardar estado en la interfaz, y que el tutorial de la Parte VIII (el 3) lo resolverá con `ViewModel`. Cierra la brecha que existía entre el anuncio del cap. 27 y el inicio del tutorial 3. `mkdocs build --strict` pasa.
- **Anexo A ampliado con nota «¿cuándo leer este anexo?»**: colocada tras la introducción, orienta a leerlo tras el cap. 10 (funciones, primera mención de DRY/SOLID) o por partes según el tema (DIP en cap. 41).
- **Anexo B reescrito como «De Java a Kotlin: equivalencias y `Scanner`»**: guía completa de equivalencias organizadas por tablas (variables y tipos básicos, funciones/métodos, clases/propiedades/constructores, *null safety*, estructuras de control, colecciones), seguida de la sección completa de `Scanner` (System.`in`, métodos de lectura, `useDelimiter`, `hasNext`). `mkdocs.yml` nav actualizado; `mkdocs build --strict` pasa.
- **Ejercicios de las Partes I–IV verificados**: los cuatro archivos de ejercicios (`part01/exercises.md`, `part02/exercises.md`, `part03/exercises.md`, `part04/exercises.md`) están completos, con enunciados, requisitos, ejemplos de ejecución y soluciones propuestas en bloques plegables. Cubren todos los temas de sus respectivas partes (variables/tipos/operadores, control de flujo/funciones, colecciones/null safety/excepciones, clases/herencia/`data class`/`enum`/`sealed class`/companion object). La Parte V (coroutines y Flow) no tiene ejercicios independientes porque la asincronía se practica mejor en contexto: los tutoriales 3, 4 y 5 ya cubren `ViewModel`/`viewModelScope`/`Flow`/`StateFlow` en aplicaciones reales.

## Tareas pendientes

- [x] Cap. 20: dejar solo genéricos y funciones de extensión
- [x] Cap. 21 (nuevo): lambdas a fondo (referencias, captura, lambdas con receptor, funciones de alcance, delegación con `by`)
- [x] Cap. 27 (nuevo): cómo se organiza una app: responsabilidades y flujo de datos
- [x] Cap. 40 (nuevo): efectos y eventos puntuales (`LaunchedEffect`, Snackbar, navegar después de una acción)
- [x] Cap. 47 (nuevo): persistencia local con Room
- [x] Tutoriales 3, 4 y 5 (ViewModel, repositorio + Hilt, Room)
- [x] Parte X: capítulos 48–58 (proyecto de contactos)
- [x] Reescrituras y ampliaciones de capítulos existentes pendientes: **38, 41, 42, 16, 17, 19, 23, 25, 26**. (Ya resueltos y quitados de esta lista: 39 y 24 en la Sesión 4; 43, 44, 45 y 46 en la Sesión 3 — ver el registro de esas sesiones arriba para el detalle de cada uno.)
- [x] Correcciones de los tutoriales 1 y 2 según el plan (el cap. 27 ya anuncia que el tutorial 2 mostrará que las tareas se pierden al girar: falta agregar ese paso)
- [x] Anexo B ampliado; anexo A con «cuándo leer esto»
- [x] Ejercicios corregidos y ampliados (Partes I–V)
- [x] Actualizar `index.md`, `README` y `CLAUDE.md` con la nueva estructura

## Notas técnicas

- **Código de la app de tareas:** `code/todo-list-app/` es la versión final (etapa 5, Room); `code/todo-list-app/etapas/` guarda las etapas 3 y 4 (ver su `README.md`). Es la fuente de los tutoriales 3 a 5.

- **Android 17 (API 37):** una app necesita el permiso de ejecución `ACCESS_LOCAL_NETWORK` para conectarse a `10.0.2.2`; sin él, OkHttp falla con `SocketTimeoutException`. Esto debe explicarse en los capítulos 45 y 49–50.
- **Compilar la app con poca memoria:** `./gradlew assembleDebug --max-workers=2 -Pkotlin.compiler.execution.strategy=in-process "-Dorg.gradle.jvmargs=-Xmx2g"`, con el emulador apagado.
- **API:** `mvnw` no tiene permiso de ejecución en el repo; usar `sh ./mvnw spring-boot:run`.
- **Verificar el sitio:** `venv/bin/mkdocs build --strict` (mkdocs está instalado en el `venv/` del repo, no en el sistema).
- **Verificar ejemplos de Kotlin de consola:** `/snap/android-studio/244/plugins/Kotlin/kotlinc/bin/kotlinc archivo.kt -include-runtime -d a.jar && java -jar a.jar`.
- **Formato de avisos:** `> [!NOTE]Nota`, `> [!TIP]Sugerencia`, `> [!WARNING]Advertencia`, `> [!IMPORTANT]`.
